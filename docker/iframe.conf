# Apache config override to allow iframe embedding
# Saves to: /etc/apache2/conf-enabled/iframe.conf
# Or mount this file into the container at that path

<IfModule headers_module>
    Header always unset X-Frame-Options
    Header always unset Content-Security-Policy
</IfModule>
